import { expect } from 'chai'

import { mergesort,
         merge } from './index'



describe('merge', () => {
  it('.........................', () => {
    expect(1).to.equal(1)
  })
})

describe('mergesort', () => {
  it('.........................', () => {
    expect(1).to.equal(1)
  })
})
