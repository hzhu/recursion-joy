import { expect } from 'chai'

import { quicksort,
         partition } from './index'



describe('partition', () => {
  it('.........................', () => {
    expect(1).to.equal(1)
  })
})

describe('quicksort', () => {
  it('.........................', () => {
    expect(1).to.equal(1)
  })
})
